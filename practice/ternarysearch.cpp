#include <iostream>
#include <stdlib.h>
#include <vector>
#include <algorithm>
using namespace std;

bool isPresent(vector<int>& arr ,int key,int first,int middle ,int last)
{
    if(key<middle)



}

int main(){
    int n ;
    cin >> n;
    vector<int> arr;
    while(n-- >0)
    {
        int p;
        cin >> p.
        arr.push_back(p);
    }
    sort(arr.begin(),arr.end());
    int key;
    cin >> key;
    cout << isPresent(arr,key,0,(n-1)/2,(n-1))<<endl;
    return 0;
}   